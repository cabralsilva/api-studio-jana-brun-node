enum StatusOfPayroll {
  OPENED = 'label.opened',
  PROCESSING = 'label.processing',
  AWAITING_APPROVE = 'label.awaiting.approve',
  CANCELLED = 'label.cancelled',
  APPROVED = 'label.confirmed'
}

export default StatusOfPayroll